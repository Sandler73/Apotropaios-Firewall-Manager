# Apotropaios Test Rules — INVALID entries for negative testing
direction=sideways action=accept protocol=tcp dst_port=443 description="Invalid direction"
direction=inbound action=destroy protocol=tcp dst_port=80 description="Invalid action"
direction=inbound action=accept protocol=ftp dst_port=21 description="Invalid protocol"
direction=inbound action=accept protocol=tcp dst_port=99999 description="Invalid port"
direction=inbound action=accept protocol=tcp src_ip=999.999.999.999 description="Invalid IP"
