# Apotropaios Test Rules Configuration
# Used by import/export tests
direction=inbound action=accept protocol=tcp dst_port=443 duration_type=permanent ttl=0 description="Allow HTTPS"
direction=inbound action=accept protocol=tcp dst_port=80 duration_type=permanent ttl=0 description="Allow HTTP"
direction=inbound action=drop src_ip=10.0.0.0/8 duration_type=permanent ttl=0 description="Block RFC1918 class A"
direction=outbound action=accept protocol=udp dst_port=53 duration_type=temporary ttl=7200 description="Allow DNS 2h"
direction=inbound action=accept protocol=tcp dst_port=22 src_ip=192.168.1.0/24 duration_type=permanent ttl=0 description="SSH from LAN"
